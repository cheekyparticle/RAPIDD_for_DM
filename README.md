# RAPIDD_for_DM

Things you need for the code to compile 

- cmake
- gsl libraries
- pkg-config

1) Go to parent directory and type 

$ sh instructions_cmake.sh

2) Then to pip install the python version of the code, type 

$ pip install -e .

3) Check that it worked by going into python and writing

>>> import rapidd

